# SonarCloud automatic analysis config (no scanner runs in CI).
# Lockfiles are machine-generated; analyzing them only yields false positives
# (e.g. S2068 flagging the "@radix-ui/react-password-toggle-field" package name
# as a hard-coded credential).
sonar.exclusions=pnpm-lock.yaml
