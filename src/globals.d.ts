/**
 * Origin of the Django API, baked in at build time by `define` in `vite.config.ts` — the
 * same `API_PROXY_TARGET` the `/api` proxy relays to. Server code only.
 */
declare const __API_PROXY_TARGET__: string;
